diff --git a/packages/coding-agent/src/core/extensions/runner.ts b/packages/coding-agent/src/core/extensions/runner.ts
index a23cd6a..c5b9318 100644
--- a/packages/coding-agent/src/core/extensions/runner.ts
+++ b/packages/coding-agent/src/core/extensions/runner.ts
@@ -245,6 +245,8 @@ const noOpUIContext: ExtensionUIContext = {
 	setWidget: () => {},
 	setFooter: () => {},
 	setHeader: () => {},
+	setRightReservedWidth: () => {},
+	setExtensionSidebar: () => {},
 	setTitle: () => {},
 	custom: async () => undefined as never,
 	pasteToEditor: () => {},
diff --git a/packages/coding-agent/src/core/extensions/types.ts b/packages/coding-agent/src/core/extensions/types.ts
index b607c7f..1110a27 100644
--- a/packages/coding-agent/src/core/extensions/types.ts
+++ b/packages/coding-agent/src/core/extensions/types.ts
@@ -189,6 +189,16 @@ export interface ExtensionUIContext {
 	/** Set a custom header component (shown at startup, above chat), or undefined to restore the built-in header. */
 	setHeader(factory: ((tui: TUI, theme: Theme) => Component & { dispose?(): void }) | undefined): void;
 
+	/** Reserve right-side columns (narrow the chat column) for a sidebar panel. Pass undefined/0 to restore full width. */
+	setRightReservedWidth(width: number | undefined): void;
+
+	/** Set a layout-level sidebar rendered on the right of the viewport (also enables chat viewport scrolling). Pass undefined to remove. */
+	setExtensionSidebar(
+		factory:
+			| ((tui: TUI, theme: Theme) => Component & { dispose?(): void })
+			| undefined,
+	): void;
+
 	/** Set the terminal window/tab title. */
 	setTitle(title: string): void;
 
diff --git a/packages/coding-agent/src/modes/interactive/interactive-mode.ts b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
index 6eba438..8477b63 100644
--- a/packages/coding-agent/src/modes/interactive/interactive-mode.ts
+++ b/packages/coding-agent/src/modes/interactive/interactive-mode.ts
@@ -37,6 +37,7 @@ import {
 	TruncatedText,
 	TUI,
 	visibleWidth,
+	wrapTextWithAnsi,
 } from "@earendil-works/pi-tui";
 import chalk from "chalk";
 import { spawn, spawnSync } from "child_process";
@@ -189,6 +190,42 @@ class ExpandableText extends Text implements Expandable {
 	}
 }
 
+/**
+ * Text component that horizontally centers each rendered line.
+ * Used for the big startup logo.
+ */
+class CenteredText extends Text {
+	private centeredWidth = 0;
+	private centeredLines: string[] = [];
+
+	override render(width: number): string[] {
+		if (this.centeredLines.length > 0 && this.centeredWidth === width) {
+			return this.centeredLines;
+		}
+		// Text.render() pads every line to `width`, which would defeat centering
+		// (visibleWidth(line) === width). Wrap + center manually instead.
+		// Centering uses the WIDEST line as the block anchor so rows of unequal
+		// width (the official logo has 6- and 8-char rows) stay left-aligned and
+		// the whole block centers as one unit (no sideways drift between rows).
+		const text = (this as unknown as { text: string }).text;
+		const wrapped = wrapTextWithAnsi(text, width);
+		let maxW = 0;
+		for (const line of wrapped) {
+			const w = visibleWidth(line);
+			if (w > maxW) maxW = w;
+		}
+		const blockLead = maxW >= width ? 0 : Math.max(0, Math.floor((width - maxW) / 2));
+		const lines = wrapped.map((line) => {
+			const w = visibleWidth(line);
+			if (w >= width) return line;
+			return " ".repeat(blockLead) + line + " ".repeat(Math.max(0, width - blockLead - w));
+		});
+		this.centeredWidth = width;
+		this.centeredLines = lines.length > 0 ? lines : [" ".repeat(width)];
+		return this.centeredLines;
+	}
+}
+
 type CompactionQueuedMessage = {
 	text: string;
 	mode: "steer" | "followUp";
@@ -325,6 +362,7 @@ export class InteractiveMode {
 	private ui: TUI;
 	private loadedResourcesContainer: Container;
 	private chatContainer: Container;
+	private sidebarComponent: (Component & { dispose?(): void }) | undefined = undefined;
 	private pendingMessagesContainer: Container;
 	private statusContainer: Container;
 	private defaultEditor: CustomEditor;
@@ -730,7 +768,24 @@ export class InteractiveMode {
 
 		// Add header with keybindings from config (unless silenced)
 		if (this.options.verbose || !this.settingsManager.getQuietStartup()) {
-			const logo = theme.bold(theme.fg("accent", APP_NAME)) + theme.fg("dim", ` v${this.version}`);
+			// Same official logo as the first-time setup screen (SETUP_LOGO_LINES
+			// in first-time-setup.ts), scaled up 9x: every char horizontally and
+			// every row vertically (4 rows -> 36 rows, chars -> 9x wide).
+			const officialLogo = ["██████", "██  ██", "████  ██", "██    ██"];
+			const bigLogo = theme.fg(
+				"accent",
+				officialLogo
+					.flatMap((line) => {
+						const scaled = [...line].map((ch) => (ch === " " ? " ".repeat(4) : "█".repeat(4))).join("");
+						return Array(4).fill(scaled);
+					})
+					.join("\n"),
+			);
+			const logo =
+				bigLogo +
+				"\n" +
+				theme.bold(theme.fg("accent", APP_NAME)) +
+				theme.fg("dim", ` v${this.version}`);
 
 			// Build startup instructions using keybinding hint helpers
 			const hint = (keybinding: AppKeybinding, description: string) => keyHint(keybinding, description);
@@ -771,16 +826,19 @@ export class InteractiveMode {
 				"dim",
 				`Pi can explain its own features and look up its docs. Ask it how to use or extend Pi.`,
 			);
+			// Centered big logo + version line, then left-aligned hints below.
+			this.headerContainer.addChild(new Spacer(1));
+			this.headerContainer.addChild(new CenteredText(logo, 0, 0));
+			this.headerContainer.addChild(new Spacer(1));
 			this.builtInHeader = new ExpandableText(
-				() => `${logo}\n${compactInstructions}\n${compactOnboarding}\n\n${onboarding}`,
-				() => `${logo}\n${expandedInstructions}\n\n${onboarding}`,
+				() => `${compactInstructions}\n${compactOnboarding}\n\n${onboarding}`,
+				() => `${expandedInstructions}\n\n${onboarding}`,
 				this.getStartupExpansionState(),
 				1,
 				0,
 			);
 
 			// Setup UI layout
-			this.headerContainer.addChild(new Spacer(1));
 			this.headerContainer.addChild(this.builtInHeader);
 			this.headerContainer.addChild(new Spacer(1));
 		} else {
@@ -2043,6 +2101,42 @@ export class InteractiveMode {
 	/**
 	 * Set a custom footer component, or restore the built-in footer.
 	 */
+	/**
+	 * Reserve right-side columns: narrow the chat column so an extension sidebar
+	 * (drawn as a right-anchored overlay) does not cover conversation text.
+	 * Width threshold matches the sidebar extension (MIN_WIDTH = 120).
+	 */
+	private setExtensionRightReservedWidth(width: number | undefined): void {
+		this.ui.rightReservedWidth = width && width > 0 ? width : 0;
+		this.ui.rightReservedMinWidth = 120;
+		this.ui.requestRender();
+	}
+
+	/**
+	 * Set a layout-level sidebar rendered on the right side of the viewport.
+	 * Also enables chat viewport mode (no scrollback; internal scrolling keeps
+	 * the sidebar fixed on screen).
+	 */
+	private setExtensionSidebar(
+		factory: ((tui: TUI, thm: Theme) => Component & { dispose?(): void }) | undefined,
+	): void {
+		if (this.sidebarComponent?.dispose) {
+			this.sidebarComponent.dispose();
+		}
+		if (factory) {
+			this.sidebarComponent = factory(this.ui, theme);
+			this.ui.setLayoutSidebar(this.sidebarComponent, 42);
+			this.ui.enableScroll = true;
+			// Wide-terminal threshold shared by chat narrowing and sidebar compositing.
+			this.ui.rightReservedMinWidth = 120;
+		} else {
+			this.sidebarComponent = undefined;
+			this.ui.setLayoutSidebar(null);
+			this.ui.enableScroll = false;
+		}
+		this.ui.requestRender();
+	}
+
 	private setExtensionFooter(
 		factory:
 			| ((tui: TUI, thm: Theme, footerData: ReadonlyFooterDataProvider) => Component & { dispose?(): void })
@@ -2173,6 +2267,8 @@ export class InteractiveMode {
 			setWidget: (key, content, options) => this.setExtensionWidget(key, content, options),
 			setFooter: (factory) => this.setExtensionFooter(factory),
 			setHeader: (factory) => this.setExtensionHeader(factory),
+			setRightReservedWidth: (width) => this.setExtensionRightReservedWidth(width),
+			setExtensionSidebar: (factory) => this.setExtensionSidebar(factory),
 			setTitle: (title) => this.ui.terminal.setTitle(title),
 			custom: (factory, options) => this.showExtensionCustom(factory, options),
 			pasteToEditor: (text) => this.editor.handleInput(`\x1b[200~${text}\x1b[201~`),
diff --git a/packages/coding-agent/src/modes/rpc/rpc-mode.ts b/packages/coding-agent/src/modes/rpc/rpc-mode.ts
index c6db8c3..e6ec276 100644
--- a/packages/coding-agent/src/modes/rpc/rpc-mode.ts
+++ b/packages/coding-agent/src/modes/rpc/rpc-mode.ts
@@ -214,6 +214,14 @@ export async function runRpcMode(runtimeHost: AgentSessionRuntime): Promise<neve
 			// Custom header not supported in RPC mode - requires TUI access
 		},
 
+		setRightReservedWidth(_width: number | undefined): void {
+			// Right panel not supported in RPC mode - requires TUI access
+		},
+
+		setExtensionSidebar(_factory: unknown): void {
+			// Sidebar not supported in RPC mode - requires TUI access
+		},
+
 		setTitle(title: string): void {
 			// Fire and forget - host can implement terminal title control
 			output({
diff --git a/packages/tui/src/tui.ts b/packages/tui/src/tui.ts
index 954256a..21fad1d 100644
--- a/packages/tui/src/tui.ts
+++ b/packages/tui/src/tui.ts
@@ -301,6 +301,33 @@ export class TUI extends Container {
 	private focusedComponent: Component | null = null;
 	private inputListeners = new Set<InputListener>();
 
+	/**
+	 * 布局级右侧面板：预留右侧列数（对话区减窄），供 sidebar 等扩展使用。
+	 * rightReservedMinWidth 为终端宽度阈值，低于它不减窄（回退全宽布局）。
+	 */
+	public rightReservedWidth = 0;
+	public rightReservedMinWidth = 120;
+
+	/**
+	 * Chat viewport mode: when enabled, only the last `height` rows are emitted to
+	 * the terminal (no scrollback). Chat history is browsed via internal scrolling
+	 * (scroll wheel / alt+pageUp / alt+pageDown), keeping a layout-level sidebar
+	 * fixed on the right side of the screen.
+	 */
+	public enableScroll = false;
+	public scrollTop = 0; // 0 = newest; >0 = lines scrolled up
+	public maxScrollTop = 0; // updated each render
+	/** Sidebar component; may accept a second render arg (viewport height) to fill the right side. */
+	public layoutSidebar: (Component & { render(width: number, viewportH?: number): string[] }) | null = null;
+	public layoutSidebarWidth = 42;
+	private mouseTrackingEnabled = false;
+	/** Cached full render (main-column lines) reused while scrolling up, so
+	 *  scrolling never re-renders the whole conversation. Invalidated on width
+	 *  change or when scrolled back to the bottom. */
+	private fullRenderCache: { width: number; lines: string[] } | null = null;
+	/** Start at the top of the conversation on the first render (show the logo). */
+	public startAtTop = true;
+
 	/** Global callback for debug key (Shift+Ctrl+D). Called before input is forwarded to focused component. */
 	public onDebug?: () => void;
 	private renderRequested = false;
@@ -343,6 +370,34 @@ export class TUI extends Container {
 		return this.showHardwareCursor;
 	}
 
+	/**
+	 * Derive the sidebar layout for the given terminal width.
+	 * Both chat narrowing (render) and sidebar compositing (doRender) share this
+	 * single source of truth, so the chat column and the sidebar column never
+	 * disagree (which would let the sidebar cover conversation text).
+	 */
+	private getSidebarLayout(width: number): { sideW: number; mainW: number; visible: boolean } {
+		const sidebarW = this.layoutSidebar ? Math.min(this.layoutSidebarWidth, Math.max(10, Math.floor(width / 3))) : 0;
+		const reserved = sidebarW > 0 ? sidebarW + 1 : this.rightReservedWidth;
+		const visible = width >= this.rightReservedMinWidth && reserved > 0;
+		const mainW = visible ? Math.max(10, width - reserved) : width;
+		return { sideW: visible ? sidebarW : 0, mainW, visible };
+	}
+
+	/**
+	 * Render children with optional right-side layout.
+	 * When a layout sidebar (or rightReservedWidth) is active and the terminal is
+	 * wide enough, children are rendered in a narrowed main column. Padding to
+	 * full width and sidebar compositing happen in doRender on the visible
+	 * window only (O(terminal height)), keeping dynamic rendering cheap even
+	 * with very long conversations.
+	 */
+	override render(width: number): string[] {
+		const { mainW, visible } = this.getSidebarLayout(width);
+		if (!visible) return super.render(width);
+		return super.render(mainW);
+	}
+
 	setShowHardwareCursor(enabled: boolean): void {
 		if (this.showHardwareCursor === enabled) return;
 		this.showHardwareCursor = enabled;
@@ -641,6 +696,9 @@ export class TUI extends Container {
 			() => this.requestRender(),
 		);
 		this.terminal.hideCursor();
+		// Enable button-event mouse tracking so the scroll wheel reaches us.
+		this.mouseTrackingEnabled = true;
+		this.terminal.write("\x1b[?1000h");
 		if (this.terminalColorSchemeNotificationsEnabled) {
 			this.terminal.write("\x1b[?2031h");
 		}
@@ -688,6 +746,10 @@ export class TUI extends Container {
 
 	stop(): void {
 		this.stopped = true;
+		if (this.mouseTrackingEnabled) {
+			this.mouseTrackingEnabled = false;
+			this.terminal.write("\x1b[?1000l");
+		}
 		if (this.renderTimer) {
 			clearTimeout(this.renderTimer);
 			this.renderTimer = undefined;
@@ -762,6 +824,32 @@ export class TUI extends Container {
 		}, delay);
 	}
 
+	/** Parse SGR/legacy wheel events. Returns +1 for wheel up, -1 for wheel down, 0 otherwise. */
+	private parseWheelEvent(data: string): number {
+		const sgr = /^\x1b\[<(\d+);(\d+);(\d+)[Mm]$/.exec(data);
+		if (sgr) {
+			const btn = Number(sgr[1]);
+			if (btn === 64) return 1; // wheel up
+			if (btn === 65) return -1; // wheel down
+			return 0;
+		}
+		// Legacy: ESC [ M <b><x><y> with button byte offset 0x60
+		const legacy = /^\x1b\[M(.{3})$/s.exec(data);
+		if (legacy) {
+			const btn = legacy[1].charCodeAt(0) - 32;
+			if (btn === 64) return 1;
+			if (btn === 65) return -1;
+		}
+		return 0;
+	}
+
+	/** Set a layout-level sidebar rendered on the right of the viewport. */
+	setLayoutSidebar(component: Component | null, width = 42): void {
+		this.layoutSidebar = component;
+		this.layoutSidebarWidth = width;
+		this.requestRender();
+	}
+
 	private handleInput(data: string): void {
 		if (this.consumeOsc11BackgroundResponse(data)) {
 			return;
@@ -798,6 +886,33 @@ export class TUI extends Container {
 			return;
 		}
 
+		// Internal chat scrolling (viewport mode): scroll wheel + alt+pageUp/Down.
+		// Keeps the layout sidebar fixed while browsing history.
+		if (this.enableScroll && this.maxScrollTop > 0) {
+			const wheel = this.parseWheelEvent(data);
+			if (wheel !== 0) {
+				this.scrollTop = Math.max(0, Math.min(this.maxScrollTop, this.scrollTop + wheel * 3));
+				this.requestRender();
+				return;
+			}
+			if (matchesKey(data, "alt+pageUp")) {
+				this.scrollTop = Math.max(0, this.scrollTop + Math.floor(this.terminal.rows * 0.8));
+				this.requestRender();
+				return;
+			}
+			if (matchesKey(data, "alt+pageDown")) {
+				this.scrollTop = Math.max(0, Math.min(this.maxScrollTop, this.scrollTop - Math.floor(this.terminal.rows * 0.8)));
+				this.requestRender();
+				return;
+			}
+		}
+
+		// Typing while scrolled up: jump back to the bottom so the editor is visible.
+		if (this.enableScroll && this.scrollTop > 0) {
+			this.scrollTop = 0;
+			this.requestRender();
+		}
+
 		// If focused component is an overlay, verify it's still visible
 		// (visibility can change due to terminal resize or visible() callback)
 		const focusedOverlay = this.overlayStack.find((o) => o.component === this.focusedComponent);
@@ -1271,8 +1386,63 @@ export class TUI extends Container {
 			return targetScreenRow - currentScreenRow;
 		};
 
-		// Render all components to get new lines
-		let newLines = this.render(width);
+		// Render all components to get new lines. While scrolled up, reuse the
+		// cached full render so scrolling never re-renders the whole conversation.
+		let newLines: string[];
+		if (this.enableScroll && this.scrollTop > 0 && this.fullRenderCache?.width === width) {
+			// Copy: the window rows below get composited (sidebar/pad) in place.
+			newLines = [...this.fullRenderCache.lines];
+		} else {
+			newLines = this.render(width);
+			if (this.enableScroll && this.scrollTop === 0) {
+				this.fullRenderCache = { width, lines: newLines };
+			}
+		}
+
+		// Chat viewport mode: emit only the visible window (no scrollback) so the
+		// sidebar stays fixed; history is browsed via internal scrolling.
+		if (this.enableScroll && newLines.length > height) {
+			this.maxScrollTop = newLines.length - height;
+			// First render: jump to the top so the big logo / welcome screen shows.
+			if (this.startAtTop) {
+				this.startAtTop = false;
+				this.scrollTop = this.maxScrollTop;
+			} else {
+				this.scrollTop = Math.max(0, Math.min(this.scrollTop, this.maxScrollTop));
+			}
+			const start = newLines.length - height - this.scrollTop;
+			newLines = newLines.slice(start, start + height);
+		} else if (this.enableScroll) {
+			this.maxScrollTop = 0;
+			this.scrollTop = 0;
+		}
+
+		// Pad window rows to full width and composite the layout sidebar on the
+		// right side. Both use getSidebarLayout geometry (same source as render),
+		// so the sidebar never covers conversation text. Only O(terminal height)
+		// rows are touched here.
+		const { sideW, mainW, visible } = this.getSidebarLayout(width);
+		const reset = TUI.SEGMENT_RESET;
+		if (visible) {
+			// Sidebar fills the full terminal height; pad the output rows so the
+			// right column spans edge to edge even when the conversation is short.
+			const sideLines = this.layoutSidebar ? this.layoutSidebar.render(sideW, height) : [];
+			while (newLines.length < sideLines.length) newLines.push("");
+			// Anchor the sidebar block to the TOP of the window (keeps it near the
+			// header / start of the conversation instead of the editor area).
+			const startIdx = 0;
+			for (let i = 0; i < newLines.length; i++) {
+				const line = newLines[i];
+				if (isImageLine(line)) continue;
+				const si = i - startIdx;
+				if (sideLines.length > 0 && si >= 0 && si < sideLines.length) {
+					newLines[i] = this.compositeLineAt(line, sideLines[si], mainW, sideW, width);
+				} else {
+					const w = visibleWidth(line);
+					if (w < width) newLines[i] = line + reset + " ".repeat(width - w);
+				}
+			}
+		}
 
 		// Composite overlays into the rendered lines (before differential compare)
 		if (this.overlayStack.length > 0) {
