diff --git a/preset/agent.cordis.yml b/preset/agent.cordis.yml
--- a/preset/agent.cordis.yml
+++ b/preset/agent.cordis.yml
@@ -28,7 +28,7 @@
 # strip the final transform, so request #1 stays Minimal-exact (see issue #6).
 #
 # V4 Pro conditions strongly on the API tool catalog AND the first request
-# output budget. Bootstrap request #1 with one native shell plus `read` and a
+# output budget. Bootstrap request #1 with one native shell plus `read`/`vision` and a
 # small maxTokens cap; after the session records its first durable promotion
 # signal (a tool call OR the first assistant message, default
 # `promoteOn: either`), later steps expose every Standard tool below and the
@@ -46,7 +46,7 @@
   name: ./tool-bootstrap.mjs
   config:
     shellTools: [bash, pwsh]
-    commonTools: [read]
+    commonTools: [read, vision]
     promoteOn: either
     bootstrapMaxTokens: 1024
     suppressedContextSources: [agent-instructions, skill-catalog]
@@ -60,7 +60,10 @@
 - id: persona
   name: '@deepseek-ai/dsh-persona'
   config:
-    text: You are a helpful software engineer assistant.
+    text: |-
+      You are a helpful software engineer assistant.
+
+      Vision fallback: you may be running on a model that cannot see images. When the user's task involves an image by file path — a screenshot, chart, document photo, or any image on disk — do not read the image yourself. Call the `vision` tool in the foreground with a short `description` and a self-contained `prompt` that includes the absolute image path and the exact question to answer. If only a filename or directory is given, locate the file with `glob`/`grep` first, then delegate the resolved path. Relay the returned text description to the user. If your model supports image input, prefer `read_image` directly.
     complete: true
     includeRuntimeContext: false
 
@@ -239,6 +242,34 @@
         toolName: subagent_fork
         backgroundMode: continuable
 
+    # vision 视觉兜底子代理:主模型无图像输入时,把图片文件路径交给
+    # image-capable 的 MiniMax M3 子代理,由它 read_image 后回传文字描述。
+    # 固定 foreground 一次性 spawn;子代理只保留定位与读图所需工具,
+    # maxDepth: 1 允许创建本子代理、但禁止它再往下委派。
+    # provider/model 必须与 dsh 中配置的 llm-pi-ai route 一致
+    # (settings.yaml -> llm-pi-ai.providers.minimax-cn)。
+    - id: tool-vision
+      name: '@deepseek-ai/dsh-tool-subagent'
+      config:
+        provider: spawn
+        toolName: vision
+        enableRunInBackground: false
+        backgroundMode: one-shot
+        maxDepth: 1
+        agentOptions:
+          provider: minimax-cn
+          model: MiniMax-M3
+        persona: |-
+          You are a multimodal vision subagent running on an image-capable model.
+          The parent agent delegated an image task to you and may not be able to see images itself. The image file path and the exact question are in the task.
+
+          1. Call `read_image` on the image path to actually see the image.
+          2. If `read_image` fails (bad path or format), use `glob`/`grep` to locate the correct file, then retry.
+          3. Describe/transcribe thoroughly and answer the parent's question directly: read out text verbatim (code, UI labels, documents, handwriting if readable); describe objects, layout, colors, and notable details.
+          4. Reply in the language of the task with focused relaying text only. Do not report tool-call details.
+        toolFilter:
+          allow: [read_image, read, glob, grep]
+
     # Product providers are host-plane singletons. Copy this preset, then
     # remove `disabled` from either ordinary tool row to expose that product
     # only to agents composed from the copy.
