/**
 * dsh-baizhu-approval — host half.
 *
 * Host 侧没有任何逻辑:审批面板接管完全由 client half(client.js)完成,
 * 它通过 package.json 的 dsh.client 声明被发现并作为浏览器插件加载。
 * 这里空的 apply 只是让本行出现在 host cordis 组合/Loader 中
 * (client-modules 只扫描已加载的 host 条目)。
 */
export const name = 'dsh-baizhu-approval'

export function apply() {}
